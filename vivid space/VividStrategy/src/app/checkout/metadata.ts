import { Metadata } from 'next';
import { COMPANY } from '../constants/company';

export const metadata: Metadata = {
  title: `Checkout - ${COMPANY.serviceName}`,
  description: `Complete your purchase of ${COMPANY.serviceName} services`,
}; 