import React from 'react';

export const metadata = {
  title: "Checkout - bettersetup Notion Setup Services",
  description: "Complete your purchase of our professional Notion workspace setup services.",
  robots: "noindex, nofollow",
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 