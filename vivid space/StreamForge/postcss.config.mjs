// This is a minimal configuration to make Next.js work with Tailwind CSS v4
// Next.js still expects a PostCSS configuration with plugins

export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}; 