import { Metadata } from 'next';
import { COMPANY } from '../constants/company';

export const metadata: Metadata = {
  title: `Checkout - ${COMPANY.name}`,
  description: `Complete your purchase of ${COMPANY.name} services`,
}; 